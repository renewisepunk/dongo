# Development validation — 2026-08-30

This is a living evidence ledger for the development stack. It is not a production release approval.

## Fixed boundaries

- Development origin: `https://dev.dongo.so`
- Convex development deployment: `wandering-camel-662`
- MCP resource: `https://dev.dongo.so/p/p58de816-dongo/mcp`
- Repository: `https://github.com/renewisepunk/dongo`
- Original PRD SHA-256: `b6a97c39aaf056dd6380e451b89fd76ff8883ac968ede5a1b0fab48eceb0f70a`
- Production `dongo.so` Worker and data were not deployed or mutated by this validation.

## Proven now

| Gate | Evidence |
|---|---|
| Local unit/integration suite | `npm test`: 239 tests passed across Workers, CLI, local storage, web, MCP, contracts, and Convex on the exact 2026-08-31 candidate. |
| Post-journey exact-candidate audit | Functional candidate `6fdb2b6` independently repeated the complete local acceptance chain: 239/239 unit and integration tests, all repository static/type checks, the production web build, generated-contract parity, six-Worker environment isolation, observability configuration, secret scanning across 375 tracked files, safe-runtime-log scanning across 185 source files, and the complete 228/228 Playwright matrix across Chromium, Firefox, and WebKit. The packaged-CLI gate produced archive SHA-256 `89541e9c2bc09872e3b656931a7e4c3aa081e0d244f83501a1956fe5edbc1337` and canonical payload SHA-256 `e0357828eccab2695ea0ffcacfb4f76945f715ecb8e2fd160ba8e0895887716e`, then passed connect/status/doctor/repeated-session/revoke/logout with owner-only `0700`/`0600` storage and no credential-helper invocation. `npm audit` reported 0 vulnerabilities. Its web-only development deploy was followed by 14/14 development smoke, 8/8 live boundary smoke, and direct unauthenticated 200 responses from both public routes. Production application routes remained absent. |
| Web browser matrix | Playwright: 228/228 passed across Chromium, Firefox, and WebKit, including auth states, onboarding, first-project device approval, MCP consent, paste image, full-page drop, comment attachments, upload retry/cancel, overview, detail, responsive, settings, authenticated and public help, public get-started, keyboard shortcuts, capture-boundary navigation, prominent copyable issue identifiers, and automated WCAG gates for both authenticated and public surfaces. |
| Static/type/build checks | `npm run check`, contract generation, lowercase brand verification, six-Worker development isolation, observability verification, the 375-file secret scan, and the 185-file safe-runtime-log scan passed on candidate `6fdb2b6`. The production build also passed. |
| Development runtime | The final coherent development deploy completed in dependency order: Convex, auth `014359fc-09f8-4cb1-9b60-7e69d20bbb80`, API `adb2090e-baf1-4fdf-a1cd-3c3c9537bece`, MCP `e84d2072-b4eb-49d8-ad89-9276659cca3f`, files `98c84e23-5bdf-4b02-939f-f6d9bdb878de`, notifications `0c10148f-ce74-4e04-b3fe-ff85a3e192a7`, and web `8ce3b725-66ff-48f6-8bbf-44daffb70d6d`. Public-guide candidate `6fdb2b6` subsequently changed only the web documentation surface and was deployed as web version `dc23e698-213d-44da-a412-55099bc3c857`; no Convex, auth, API, MCP, files, notifications, or production resource was changed. `npm run smoke:dev -- --project-ref p58de816-dongo`: 14/14 passed after that deployment. Notification readiness explicitly requires dispatch plus Resend for Web Beta and still reports APNs/FCM as disabled. |
| Live environment isolation | Wrangler deployment history resolves the development web, auth, API, MCP, files, and notification routes to their separate Workers, all bound only to `dev.dongo.so` and the development resources recorded above. The repeatable `npm run smoke:boundaries -- --project-ref p58de816-dongo` gate passed 8/8: development web/auth were live, development MCP required authentication, production auth/API/MCP paths returned 404, the production landing returned 200, and `www.dongo.so` redirected once to the production apex. No deployment or mutation is performed by this gate. |
| Packaged CLI in this repository | Packed `@dongo/cli@0.1.0` (SHA-256 `6716a8acfa1b0dd6d700d8182eef58dfc7d070191b8bc590b108320473f78b00`), installed that tarball into a clean temporary prefix, and ran the exact binary from this repository. `doctor`, `session-start`, and `overview` succeeded against the live project. The packed CLI then created `DONGO-4`, started it, recorded a live update, and finished it. Create, start, update, and finish were each replayed with the original revision and fixed idempotency key; all replays returned the same record, run, revision, and timestamps without a duplicate application. The completed Claude Code and packed-CLI lifecycle items plus the generic compatibility item were exported into `.agent-work`; a second `sync` returned the same four file hashes and no removals. |
| Repository metadata | Live `doctor` returned repository URL `https://github.com/renewisepunk/dongo`, project ref `p58de816-dongo`, and matching server installation context. |
| CLI credential policy | Live CLI uses the owner-only dongo credential file and produces no Keychain/helper prompt. Focused tests now directly cover simulated foreign directory/file ownership, non-regular and symlink paths, broad modes, repository escape, corrupt and unsupported records without secret reflection, crash-style temporary writes preserving the committed credential, refresh rotation, and failed-revocation retention. The immutable-package verifier also shadows macOS Keychain and Linux Secret Service helper entry points with fail-closed shims, then completes connect, repeated authenticated commands, revocation, and logout without invoking any helper. |
| Codex MCP human/agent lifecycle | A disposable repository-scoped Codex installation `j574bbhnbnj63qb2xhthmktjm98dgkpg` completed the full live development loop as itself. From human Intake `kh7ffx32hrj29m1qhmez815x3h8dhdvy`, Codex claimed, created, triaged, started, and updated `DONGOE2E-2`; it requested important Attention `jn7er972zzd2vhwvndhvwhqdss8dgcn8`. The human selected `Approve completion` and submitted an attributed response in the live UI. Codex's next `dongo_session_start` returned the exact resolution, after which Codex resumed in Run `m972xxk3xwwj9w2vvdcy1q2pdh8dht0v`, published a final Markdown/emoji comment, finished revision 5 with no stale active Run or open Attention, and attached report metadata `jd70jg465ahg6h21e36yrgrb4n8dhj85`. Claim, create, triage, start, both comments, Attention, and finish were each replayed with their original stale revision/idempotency key and returned the original IDs, timestamps, and revisions without duplicate application. After the final coherent deploy, the authenticated live UI rendered the answered human decision, attributed human and Codex comments, Markdown checklist/emoji, outcome, artifact label, Done state, and accessible copy control for `DONGOE2E-2`. |
| Cross-surface repository proof | Packaged-CLI sync exported agent-authored work and comments into `.agent-work` without temporary URLs. After the live multipart journey, the installed CLI exported the human video-verification note and attachment ID `jh74dfz47hprqmjb7ht0s0qxdh8dhts5` into `work/DONGO-3-generic-mcp-inspector-compatibility-verification.md`; the manifest recorded SHA-256 `8b4a36461997e9f8d35f3f8f80c05adf9a5cf2e19c77e5b1a7824318fa05666e`. A second sync returned the same four paths and hashes with no removal or diff. |
| Google redirect configuration | The live Google start reached Google's normal account identifier screen instead of `redirect_uri_mismatch`. Its callback target was exactly the development Convex auth callback. No Google credentials were entered during this probe. |
| Fresh-account agent-first CLI | A new disposable `test@paul9.com` identity completed email OTP with no existing project. The packed CLI proposed `dongo e2e`, `https://github.com/renewisepunk/dongo`, and manual execution in one complete device link; the browser showed the fixed proposal with the same comparison code and no project picker. `Create & approve` provisioned project `hn8dewzz-dongo-e2e`. A cross-account browser-session mismatch surfaced during the first grant attempt, was fixed with exact Convex-profile binding, covered by focused tests, deployed, and then retested successfully. The browser showed `Approved — you can close this window`; the waiting packed CLI stored an owner-only local credential, wrote the repository marker, and passed `auth status`, `doctor`, `overview`, and `session-start` against the new empty project. Both files were mode `0600`; no Keychain/helper prompt appeared. |
| OAuth callback safety | MCP consent uses `window.location.assign` for a top-level host-owned loopback redirect. dongo does not frame, fetch, probe, or proxy localhost. Live Claude Code authorization returned directly to the host-owned loopback completion page in Chrome without triggering the private-network/device-access prompt. |
| Claude Code MCP compatibility | The deployed authorization Worker accepts only Claude Code's exact CIMD client and exact port-bearing localhost callback shape after verifying the official metadata's base callback. The callback remains bound through both consent and continuation POSTs and is revalidated before use. Auth tests cover the positive path and rejected host, scheme, path, query, client, duplicate, malformed, and oversized inputs. Live consent identified `Claude Code` exactly; the grant appears as `Claude Code remote MCP`, and Claude Code reported the server connected. A distinct Claude Code installation called `dongo_session_start` and `dongo_get_overview`, then created work item `DONGO-2` once. Repeating that create request with the same idempotency key returned the same item without a duplicate. Under a later explicit manual-mode validation instruction, Claude Code started, updated, and finished only `DONGO-2`; each mutation was replayed with its original now-stale revision and the same idempotency key. Every replay returned the same run or work result without a revision bump, timestamp drift, or duplicate application. The packed CLI independently read the completed item as `done` at revision 4 with the expected outcome. On candidate `0b0020d`, the installed Claude Code CLI again reported the remote server connected and completed a fresh restricted read-only `dongo_session_start` plus `dongo_get_overview`; dongo identified the caller as display name `Claude Code`, agent type `mcp`, on project `p58de816-dongo` in manual mode. |
| Claude Code human Attention lifecycle | Claude Code completed `DONGO-5` (`mh7ae2xsy7eysn2516hf13yp9x8dh9ss`) as installation actor `j5795mpab02d487c5p73maf5e18dfrf2`, display name `Claude Code`, `agentType: mcp`. Human Rene Lonngren answered review Attention `jn72fppzc8609ewqwfhefetymn8dg6cs` with `Approve — continue and finish`; the attributed response comment is `js700n1aqka9nbnv140mh7zpbs8dgrqf`. The stopped agent received that resolved decision on the next explicit session start, created fresh continuation Run `m97bnh3mn9wms9v1d786s8g0kd8dgrvt`, published final Markdown comment `js7etn8ka3gkx6w75fk8n2ffe18dgxdh`, and finished at revision 6 with report artifact `jd7a3tjb2z95czstavjfr5hfc58dhcxj`, no active Run, and no unresolved Attention. Continuation `start_work`, `add_comment`, and `finish_work` were each repeated with their original stale revision and fixed keys `dongo-5-continuation-start-20260831`, `dongo-5-continuation-comment-20260831`, and `dongo-5-continuation-finish-20260831`; every replay returned the original ID, timestamps, revision, result, outcome, and artifact with no duplicate or revision bump. The authenticated live UI rendered Done, the human selection, separate human/Claude attribution, the Markdown table, and the report. Two installed-CLI syncs returned the same five paths and no removals; `DONGO-5` exported with SHA-256 `537c0144798e49bf98592d4f1b662135943f7d91d1b055d1a68a565d135cb843`. |
| Generic MCP Inspector compatibility | MCP Inspector 2.4.0 completed OAuth against the project-scoped resource and strict tool discovery returned the complete portable catalog. Its distinct installation identified itself as `MCP Inspector`, called `dongo_session_start` and `dongo_get_overview`, and created Ready work item `DONGO-3` once. Repeating the same `dongo_create_work` input with the same idempotency key returned the same item ID, timestamps, and revision without a duplicate. After the isolated access credential was deliberately invalidated, a stored-auth-only overview call refreshed non-interactively, rotated the access credential, retained refresh capability, and remained bound to the exact issuer. The disposable Inspector grant was then revoked; its next stored-auth-only request failed with `auth_required`. A fresh Claude Code MCP session/read and a packed-CLI overview both succeeded afterward, proving the revocation did not affect either retained installation. `DONGO-3` remains unclaimed and unstarted in manual mode. |
| Lowercase product name | Static copy verifier now covers root source, JS/TS strings, prose, HTML, JSON/JSONC, SQL, SVG, and XML. Legacy CLI system labels were migrated idempotently in Convex and auth D1; user-created project names remain verbatim. |
| Important Attention delivery | Live request `jn7126d0h40h05fb0cr3etdbtn8dfg8p` created delayed outbox item `ks7dka8784aha3ps7ynv8hhj0n8de289`. It dispatched once at the real one-hour boundary with no error or retry. Convex recorded provider message `b266c07d-0769-41dd-8723-93fb1a967ec0`; the Wisepunk Resend account reported that same message delivered from `dongo <notifications@dev.dongo.so>` with subject `Attention still needed for DONGO-1`. |
| Exact-candidate self-host check | After the coherent 2026-08-31 deploy, the workspace-installed `/opt/homebrew/bin/dongo` passed `auth status`, `doctor`, and `session-start` against `p58de816-dongo`. The server matched the repository marker and installation, returned `dongo CLI` with `agentType: cli`, and returned MCP-authored updates as the distinct `MCP host` actor with `agentType: mcp`. |
| Exact-candidate CLI package provenance | `npm run verify:cli-package` makes the immutable-package proof an executable CI gate. It packs `dongo-cli-0.1.0.tgz`, requires the archive to contain only the intended package files and no source, dependency tree, environment file, unsafe path, or credential-helper integration, installs it into an isolated prefix, and invokes that installed binary in a clean Git repository with `DONGO_TOKEN` removed. The installed archive then completes a local mock device grant, authenticated status, `doctor`, a second `session-start`, server-first revoke/logout, and final unauthenticated status. It proves the external credential is the only config file and mode `0600`, the repository marker contains no authorization material, and fail-closed shims for macOS Keychain and Linux Secret Service tooling were never invoked. The accepted pre-gate archive used for the live CLI journeys remains SHA-256 `e04fcc2fcc10e552417c046450531936985ff89b5b03504bcd2848f4add30990`; new release packages include the provenance instructions and pin canonical file contents through `apps/cli/package-payload.sha256`. The current macOS archive is SHA-256 `89541e9c2bc09872e3b656931a7e4c3aa081e0d244f83501a1956fe5edbc1337`. A fresh `node:20-bookworm-slim` container copied the exact candidate without host dependencies, installed the lockfile, and passed the same complete lifecycle with Linux archive SHA-256 `7273079903a851aa1afb9f2f42e149548db87694ad07ead4590c38d4c554844e`. Both environments produced canonical payload SHA-256 `e0357828eccab2695ea0ffcacfb4f76945f715ecb8e2fd160ba8e0895887716e`; npm archive envelopes may differ while their pinned contents must not. GitHub CI runs the same lifecycle gate as a dedicated clean Ubuntu/macOS matrix on the minimum supported Node 20 while the main suite remains on Node 24. |
| Clean Linux package and headless auth | Mounted the exact immutable archive (SHA-256 `e04fcc2fcc10e552417c046450531936985ff89b5b03504bcd2848f4add30990`) read-only into fresh `node:20-bookworm-slim` containers, installed it globally without workspace source, initialized `/work` for `https://github.com/renewisepunk/dongo`, and ran it on Node `v20.20.2`. The real `--no-browser` one-link flow was approved for `test@paul9.com` and project `hn8dewzz-dongo-e2e`; polling honored server `slow_down` intervals of 10, 15, 20, and 25 seconds. Installation `j57d98sa78jx0x05np48x5xqhx8dfvsk` reported `credentialStore: local-user-file`, authenticated with refresh capability, and passed `doctor` plus `session-start`. The config and credential directories were mode `0700`; credential and marker files were `0600`; `security`, `secret-tool`, and `dbus-launch` were absent. `auth logout` returned `revoked:true`, removed the credential, and the final status was unauthenticated with no refresh credential. A separate unapproved clean-container request then remained pending through its fixed ten-minute lifetime, honored the same progressive `slow_down`, and terminated with exit code 3, `authorization_expired`, and `retryable:false`; its config directory stayed empty at `0700` and no repository marker was created. Finally, the current packed archive (SHA-256 `89541e9c2bc09872e3b656931a7e4c3aa081e0d244f83501a1956fe5edbc1337`) was installed into another fresh Debian/Node 20 container and created comparison code `KEW3-4NJ8`. The live browser showed the exact `test@paul9.com` account, `Personal workspace / dongo e2e` project, official CLI client, and agent API resource. Explicit `Deny` produced the branded no-token completion page; the terminal exited 3 with `authorization_denied`, the config directory remained empty at mode `0700`, and no repository marker was created. |
| Automated accessibility gate | `@axe-core/playwright` 4.13.0 scans the authenticated Overview and route-backed work detail in Chromium, Firefox, and WebKit with reduced motion. The enforced rule sets cover WCAG 2.0, 2.1, and 2.2 levels A and AA. The first run found and drove fixes for the unlabeled Intake file input and insufficient Ready-position contrast; the final three-browser gate passed with no detectable violations. |
| Live CLI/human golden journey | On the final deployed candidate, the live web created Intake `kh7486jc5ez4kttq54qb9q8qrx8dhxp7`; the reauthorized isolated `dongo CLI` claimed it, created and linked `DONGOE2E-1`, completed triage, started truthful Working, and published a Markdown/emoji progress update as itself. It requested important review `jn72encef8fwk4wev1a17kpf218dh124`; the human selected `Approve completion` and added an attributed response in the live UI. The next `session-start` returned that exact resolution, the CLI resumed in a new Run, finished at revision 7 with report artifact `jd77yg05543zrzezrb26ftbyrx8dgw4r`, and `sync` wrote `work/DONGOE2E-1-validate-the-end-to-end-development-loop.md` with SHA-256 `1bed4d1f1a422beb5f9b5955ad40993c1291754aef0fc13040793cb535fb8edd`. |
| Truthful Run state after Attention | The live golden journey exposed that a completed WorkItem could still serialize an older resolved-Attention Run as `activeRun`. The read model now exposes a Run as active only when it owns the current lease or is tied to unresolved Attention. A regression covers response → next pull → resumed Run → finish; the final deployed read of `DONGOE2E-1` is Done with the correct outcome and no `activeRun`. |
| Overview keyboard and selection treatment | The capture composer participates in list navigation: `K`/Up from the first work item focuses capture and Down returns to the first item. Ready and Done selections render one continuous outer amber border without nested focus boxes. This was verified directly on the deployed `dongo e2e` project as well as by the focused Chromium case and complete 228-case browser matrix; the web asset is included in deployed web version `8ce3b725-66ff-48f6-8bbf-44daffb70d6d`. |
| Prominent issue identifiers | Work detail presents the issue ID in high-contrast monospace directly above the title with an adjacent copy control. The control copies only the identifier, announces success without exposing exception details, and returns to its idle state automatically. `DONGOE2E-1` was visually verified on the deployed `dongo e2e` project; clicking it produced the green confirmation state and `DONGOE2E-1 copied`. The exact placement, computed size, clipboard value, feedback, and copied state pass in Chromium, Firefox, and WebKit. |
| Public get-started and help | `/get-started` and `/help` are static, unauthenticated routes in candidate `6fdb2b6`. They explain source installation, one-link terminal → browser → terminal authorization, first-project creation without prior app setup, separate Codex/Claude/generic MCP grants, connection verification, host-specific re-authentication, server revocation versus local configuration removal, the owner-only local credential file and no-Keychain policy, everyday agent/human workflow, attachment paste/drop/comment behavior, shortcuts, resource links, and bounded recovery. Both routes pass focused navigation and signed-out coverage plus Axe WCAG A/AA scans in Chromium, Firefox, and WebKit as part of the exact candidate's complete 228/228 Playwright matrix. After web-only development deployment, both routes returned HTTP 200 without cookies or redirects and contained the expected lifecycle content. |
| Live 200% zoom and keyboard reflow | Chrome's native zoom was increased from 100% to 200% on the authenticated development Overview and `DONGO-1` detail. The CSS viewport reduced from 1800 to 900 pixels while document and body scroll widths remained within the client width, so neither view introduced horizontal page overflow. The accessibility tree retained named navigation, capture, work, detail, copy-ID, comment, and attachment controls. Keyboard focus cycled inside detail through close → copy ID → comment → attach → close. Chrome was then restored to 100%. This is high-zoom and keyboard evidence, not a substitute for the pending screen-reader and measured-performance audits. |
| Live browser-to-R2-to-MCP attachment | With explicit approval, the authenticated development UI added `apple-touch-icon.png` plus the comment “Live browser → R2 → MCP attachment verification. This synthetic image was submitted with explicit approval.” to `DONGOE2E-2`. The live detail rendered the human actor, filename, `2.2 KB · secure download`, and download control. A separate read-only Codex MCP session fetched Work `mh7bfyfazba8sypwnpxev69vys8dg0qy`, resolved comment `js7a7064yyrpkq5f9713brqgf58dg6gw` and attachment `jh74qfqg4t0nppswqj9csyn3718dh2t9`, requested the attachment through the MCP capability, and downloaded 2,301 bytes without retaining or reporting its temporary URL. The downloaded and local source SHA-256 values both equal `b1f21fabbfea29df8b69ad69394e0dd643836a27801465425563cf8383d37352`. This proves the deployed browser direct-upload → R2 finalization → human comment → agent retrieval path for an image. |
| Live interrupted multipart video | With explicit approval, the authenticated development UI selected the synthetic H.264 screen recording `dongo-multipart-screen-recording.mp4` for a comment on `DONGO-3` (Work `mh79k7q07ja35gex7jabz925wx8dgc0m`). Its 44,407,621-byte size forced the multipart path. The first attempt reached the visible `Upload interrupted. Retry when you are online.` state without finalizing a comment; the same draft's `Retry` action then completed as `42 MB · ready`. The human submitted comment `js7bs7crnwqsn16afnhcs91dxh8dgavp`, and the live detail rendered `Rene Lonngren`, `HUMAN`, the exact body, video filename, `42 MB · secure download`, and download control. A fresh, restricted Claude Code session used only dongo MCP read tools to resolve the same human comment and attachment `jh74dfz47hprqmjb7ht0s0qxdh8dhts5`, reporting `video/mp4`, 44,407,621 bytes, and an attachment resource without exposing its temporary URL. An independent authenticated agent fetch downloaded the finalized R2 object, then deleted the temporary copy. Source and downloaded SHA-256 values both equal `e1c9d0a23759da0d428ad621d72be345daa82bcae93f7a20a9de5a7a118db033`. This proves the deployed interrupted multipart → retry → R2 finalization → human comment → MCP discovery → byte-exact agent retrieval path for video. |
| Development observability | CI requires explicit persistent invocation logs at rate 1 and persistent trace sampling at 0.01–0.05 across all six development Workers. The coherent deployment above carries that verified configuration; a live CLI `doctor` passed against the new API, while the prior filtered tail probe on version `43d558d2-0989-4340-aae6-56a914c6d41a` recorded outcome `ok`, HTTP 200, redacted Authorization, and no application logs or request body. Broad invocation output contains private network metadata and was not retained. The active `rene@wisepunk.com` Wrangler OAuth profile still lacks Notifications Read/Write. A read-only authenticated dashboard audit nevertheless inspected all six existing unrelated policies and all 54 available types: no dongo policy or Workers alert can filter to `dev.dongo.so`; the only exact-host candidate is a Health Check transition notification, and the `dongo.so` Health Checks page requires a Pro/Smart Shield upgrade. No plan, policy, recipient, or Health Check was changed. Development-only external alert routing therefore remains explicitly incomplete rather than being replaced with a broad production-mixing policy. |

## Still required

- Complete live Google sign-in with the intended Wisepunk Google account and verify the resulting Convex identity; the redirect-only probe is not full authentication proof.
- After any further code change, rerun the complete suite and repeat secret, runtime-log, contract, environment, browser, and live smoke gates on the exact candidate commit. The exact 2026-08-31 candidate passed these gates before its coherent development deployment; both live smoke suites passed again afterward.
- Native APNs/FCM, native clients, production isolation/promotion, the clean-host browser-approved OAuth/lifecycle matrix, performance/accessibility audits beyond the current automated matrix, rollback rehearsal, and product-owner sign-off remain separate V1 gates. Clean Linux package installation itself is proven above.
