# V1 completion audit — 2026-08-31

This audit measures the current repository and live development stack against the unchanged PRD and [`build-plan/03-release-gates.md`](../../build-plan/03-release-gates.md). It is intentionally stricter than a progress summary.

The original PRD SHA-256 remains `b6a97c39aaf056dd6380e451b89fd76ff8883ac968ede5a1b0fab48eceb0f70a`.

## Current build-goal boundary

The product owner narrowed the active build goal on 2026-08-31 to an end-to-end development candidate. Native iOS and Android applications, push-notification delivery, staging, and production application deployment are explicitly outside this goal. Responsive mobile-web behavior remains in scope. This is an execution boundary for the current goal, not a change to the original PRD.

Rows below that concern native clients, staging, or production are retained as an honest account of the full PRD, but they do not block completion of the current dev-only goal.

## Current verdict

The development web, CLI, canonical operation layer, remote MCP server, edge services, email notification path, and repository export are substantially implemented and tested. The exact current candidate passes the automated suite recorded in [`dev-validation-2026-08-30.md`](dev-validation-2026-08-30.md).

The current dev-only goal is **not yet complete**. The uninterrupted CLI/human, Codex MCP/human, and generic MCP Inspector/human golden journeys are proven; Claude Code has completed both Intake triage and the equivalent work/Attention/continuation portion. The intended Wisepunk Google identity is proven live, deployed browser image and interrupted-multipart video → R2 → MCP/agent retrieval journeys have byte-exact hash verification, the owning test account has visually proven the deployed lazy Blob-backed raster preview, both the public root and authenticated workspace have measured Chrome performance traces, and the exact packed CLI has completed real one-link authorization plus revocation from isolated macOS and clean Linux environments. Its remaining evidence includes Claude and Codex refresh/revoke/reauthorize, manual screen-reader review, and activation/delivery proof for the bounded development availability monitor. Native clients, push, staging, rollback rehearsal, production application deployment, and the broader distribution-host matrix remain outstanding against the full PRD but are outside the current dev-only build goal.

## Requirement-by-requirement status

| Gate | Status | Authoritative evidence | What remains |
|---|---|---|---|
| Original scope preserved | Proven | The PRD digest matches the recorded baseline. | Keep the PRD unchanged; record implementation decisions only in additive documents. |
| Human email authentication | Proven in development | A fresh account completed email OTP, first-project provisioning, and CLI approval on the live development stack. Automated success, failure, resend, callback, and rate-limit journeys pass. | Repeat on the final staging/production candidate. |
| Human Google authentication | Proven in development | The live Google account chooser offered both known accounts; the human explicitly selected `rene@wisepunk.com`. The exact callback returned to the authenticated development Overview. The profile menu rendered `Rene Lonngren`, `rene@wisepunk.com`, and owner. A read-only Convex development audit found the same existing human profile updated by the login and confirmed its owner membership in organization `Rene Lonngren` (`rene-lonngren-mwqs8df83k`), which owns the current project route. The `rene@twise.ai` work account was not used. | Repeat on the final staging/production candidate. |
| One-link CLI authentication | Proven in development, isolated macOS, and clean Linux headless mode | The packed CLI completed terminal → browser → authenticated terminal with no token copy, project picker, Keychain prompt, or repository credential. The exact current macOS archive SHA-256 `89541e9c2bc09872e3b656931a7e4c3aa081e0d244f83501a1956fe5edbc1337` was installed into a new prefix and authorized as `rene@wisepunk.com` for `p58de816-dongo`; status, doctor, session start, overview, server revocation, credential removal, and final unauthenticated status all passed. Exact packed archives separately repeated `--no-browser` authorization in fresh Debian/Node 20 containers, honored progressive polling slowdown, and proved expiry and explicit denial. The lowercase success copy is deployed in development web version `6f77a91a-2e12-4174-9f61-0b8b599f2d71`; its exact candidate passed 239/239 tests, 246/246 cross-browser cases, 14/14 live smoke, and 8/8 boundary checks. | Broader Linux desktop, WSL, and release-blocked Windows coverage remains a distribution gate outside the current dev-only goal. |
| CLI credential storage | Proven on isolated macOS, clean Linux SSH/container, and in automated tests | Owner-only external credential files, atomic refresh rotation, symlink/mode/ownership/corruption failures, and failed-revocation retention pass. The real isolated macOS grant used only `0700` directories and `0600` credential/marker files; its marker had no authorization material, fail-closed Keychain/Secret Service shims were never invoked, and logout removed the credential while retaining the non-secret marker. The clean Linux grant proved the same owner-only local-file policy with no OS helper installed. | Complete the broader Linux desktop, WSL, and release-blocked Windows distribution matrix. |
| Codex remote MCP | Proven through the human/agent lifecycle | A distinct Codex installation completed repository-scoped OAuth, Intake claim, replay-safe create/triage/start/comments/Attention/finish, human response, next-pull continuation, attributed Markdown progress, report metadata, and truthful terminal state with no stale active Run. The isolated test-project CLI then performed two identical syncs: `DONGOE2E-1` SHA-256 `1bed4d1f1a422beb5f9b5955ad40993c1291754aef0fc13040793cb535fb8edd`, `DONGOE2E-2` SHA-256 `53e365a96d49b16ccf2e4e51e1933113e069c5afac97b64f09df37f803399085`, and no removals. The exported image reference is an opaque attachment ID with no temporary URL. | Prove refresh/revoke/reauthorize on a disposable Codex grant. |
| Claude Code remote MCP | Proven through Intake triage plus the human/agent work and Attention lifecycle | A distinct Claude Code installation completed OAuth, session/read, replay-safe create/start/update/finish, and remained active after another installation was revoked. On `DONGO-5`, Claude Code then requested normal human review, stopped, pulled Rene Lonngren's attributed `Approve — continue and finish` response on a later session, started a fresh Run, published an attributed Markdown report, and finished truthfully at revision 6 with no stale active Run. Replaying continuation start, comment, and finish with the same idempotency keys and stale revisions returned the original IDs, revisions, timestamps, outcome, and report artifact without duplication. Two CLI syncs exported the same file hash `537c0144798e49bf98592d4f1b662135943f7d91d1b055d1a68a565d135cb843`. After the account allowance reset, an explicit single-server restricted Claude session called `dongo_session_start` exactly once as `Claude Code` / `agentType: mcp` for `p58de816-dongo` in manual mode with no permission denial or mutation. Human Intake `kh77hbkjmnhee01gzankv4pby58dhy76` then completed the missing triage gate: Claude claimed it, created exactly one linked Ready `DONGO-7`, completed it as processed, and replayed all three mutations without duplicate IDs or revision bumps. Independent CLI state showed an empty Inbox, one Ready linked item, no comments or artifacts, and no Working item. | Prove refresh/revoke/reauthorize on a disposable Claude grant. |
| Generic remote MCP | Proven through the complete human/agent lifecycle | MCP Inspector 2.4.0 completed strict discovery and project-scoped OAuth as its own `agentType: mcp` installation Actor. It claimed human Intake, replayed claim/create/triage/start without duplication, entered truthful Working, published attributed Markdown, requested human review, stopped, and received Rene Lonngren's attributed `Approve — continue and finish` only on a later explicit `dongo_session_start`. Fresh continuation Run `m972yv0shqqtke3nt2tgekfncs8dgmnx` then published final comment `js7b69h88jef5jjgma9mk08b258dg52s` and finished `DONGO-6` at revision 5 with report artifact `jd7cpeb2jttsj7sq55z9b5nhsn8dhyc2`. Exact stale-revision replays returned the original Run, comment, artifact, timestamps, and revision. Two installed-CLI syncs exported the same six paths with no removals; the `DONGO-6` file SHA-256 is `14dbf5379dfe329916735e695b3c15c7abd7cd83e0cb250ca59d813e2ab79196`. A prior disposable Inspector grant separately proved non-interactive refresh, immediate revocation, and isolation from Claude and CLI. | Revoke only the completed disposable Inspector grant after explicit product-owner approval; no additional lifecycle evidence is required unless this integration changes. |
| Canonical domain and concurrency | Proven by automated suite | Contract, Convex, API, MCP, client, claim, lease, revision, idempotency, response-loss, tenancy, and lifecycle tests pass on the current candidate. | Repeat stress and failure-injection gates on staging with the exact release artifacts. |
| Packed CLI lifecycle | Proven in development; real isolated macOS and clean Linux install/auth/logout proven | The exact packed archive created, started, updated, and finished `DONGO-4`; every mutation replay returned the original result. A second deterministic sync returned identical hashes. A separate disposable grant proved logout/revoke isolation, and a final isolated CLI run completed live Intake triage, important human Attention, next-pull continuation, finish, artifact, and deterministic export as `DONGOE2E-1`. Two later syncs from that same isolated repository returned exactly the same `DONGOE2E-1` and `DONGOE2E-2` paths and SHA-256 values with no removals; the attachment export retained only its opaque ID. The accepted immutable archive installed in fresh Debian/Node 20, completed real headless OAuth, passed authenticated checks, and revoked cleanly without a credential helper. Exact macOS archive SHA-256 `89541e9c2bc09872e3b656931a7e4c3aa081e0d244f83501a1956fe5edbc1337` then independently completed real development connect/status/doctor/session/overview/revoke/logout from a new prefix, config root, and repository while fail-closed helper shims remained untouched. | The full work lifecycle inside clean Linux and the broader distribution-host matrix remain outside the current dev-only release proof. |
| Web product behavior | Proven in automated browser matrix; public guides, live high-zoom, image, interrupted-multipart video, and public/authenticated lab performance proven | Exact functional candidate `6fdb2b6` passed 228/228 Playwright tests across Chromium, Firefox, and WebKit, including Overview, detail, responsive states, paste image, full-page drop, comment attachments, upload retry/cancel, search, settings, authenticated help, public get-started/help, keyboard shortcuts, capture-boundary navigation, prominent copyable issue identifiers, auth/consent states, and automated WCAG 2.0/2.1/2.2 A/AA scanning for authenticated and public surfaces. Current image-preview candidate `778eaed` independently passes 247 unit/integration tests and 249/249 Playwright cases. On the authenticated development candidate, native Chrome 200% zoom preserved named controls, trapped detail focus, and introduced no horizontal page overflow at the resulting 900 CSS-pixel viewport; Chrome was restored to 100%. Controlled Chrome traces measured the optimized public root at 714 ms mobile LCP and 0.00 CLS under Slow 4G plus 4× CPU, while three authenticated 1440×960 runs under 4× CPU and constrained networking produced median 2,348 ms LCP, 0.00 CLS, no long tasks, and median 30/84 ms keyboard selection/open timings. Live human comments uploaded an image to `DONGOE2E-2` and a 44,407,621-byte H.264 screen recording to `DONGO-3`; the video entered the visible interrupted state, succeeded through its draft retry action, and rendered its secure-download state. Development web version `31a1ad31-e125-4b17-9ccc-7011edf09efe` adds secure lazy inline raster previews with explicit download and fail-closed generic-card fallback, then passed 14/14 live smoke and 8/8 boundary checks. | Complete the repeatable manual VoiceOver review in [`manual-screen-reader-2026-08-31.md`](manual-screen-reader-2026-08-31.md). |
| Public root and desktop issue workspace | Proven in development | Candidate `78c476d` makes `/` a static public marketing page, keeps authenticated root resolution at `/open`, and replaces the narrow wide-screen sidebar detail with a compact issue navigator plus spacious non-modal detail. Left returns from detail to the selected issue; Up/Down selects without opening; Enter opens and focuses the chosen detail. The exact candidate passed 246/246 Playwright tests across Chromium, Firefox, and WebKit, including authenticated/public WCAG A/AA scans. A signed-in Chrome session repeated the complete DONGO-1 → Left → Down → Enter → DONGO-3 focus journey against deployed development web version `4dd58f93-8c13-4c41-a0d4-bd4e86a8d424`. Development smoke passed 14/14 and the environment boundary gate passed 8/8; no production application route was deployed. The optimized public root measured 714 ms mobile LCP and 0.00 CLS under Slow 4G plus 4× CPU; the authenticated workspace separately passed the controlled interaction/throttling gate. | Complete the manual VoiceOver review. |
| Sidebar keyboard-target visibility | Proven in development | Follow-up `9b79262` separates the open issue from the pending keyboard target: the open issue retains a quiet amber rail, while the focused Up/Down target receives a full 2px amber frame, amber-tinted surface, and brighter identifier until Enter moves focus into detail. The exact candidate passed web checks, 34/34 unit tests, 9/9 focused cross-browser navigation tests, and 246/246 complete Playwright tests. A signed-in live Chrome screenshot showed DONGO-1 open and DONGO-3 visibly framed before activation on development web version `653b0b8a-9d50-41e9-a5c2-0089fdc5ae5f`; smoke remained 14/14 and boundaries 8/8. The authenticated interaction trace subsequently measured the same navigation path under constrained conditions. | Complete the manual VoiceOver review. |
| Bidirectional issue/detail focus | Proven in development | Candidate `e168854` makes Left return from wide detail to its open sidebar row and return from any focused sidebar issue to the issue already open, without activating a different pending row. It resets subsequent action context to the open issue and exposes the same documented command-menu action. Web checks, 34/34 unit tests, focused 6/6 cross-browser tests, and the exact 246/246 Playwright matrix passed. Follow-up `4e02deb` makes `R` focus the existing open issue's review/comment surface instead of trying to remount the same detail. Its web typecheck and focused Chromium, Firefox, and WebKit tests passed. A signed-in Chrome journey kept DONGO-3 and its URL open while DONGO-1 was the visible keyboard target, then proved Left focused DONGO-3 detail and `R` focused `Add a comment…` on development web version `8cb1341c-97ef-43e5-8c1d-6fbc44ee17eb`; smoke passed 14/14 and boundaries 8/8. The authenticated interaction trace subsequently measured the complete J/Enter open path under constrained conditions. | Complete the manual VoiceOver review. |
| Attachments and R2 | Proven live for direct image and interrupted-multipart video upload, MCP discovery, byte-exact agent retrieval, deterministic export, and secure raster preview behavior | The deployed browser submitted a text comment with `apple-touch-icon.png` to `DONGOE2E-2`, rendered its secure-download state, and finalized it to R2. A read-only Codex MCP session resolved attachment `jh74qfqg4t0nppswqj9csyn3718dh2t9`, downloaded all 2,301 bytes, and matched the source SHA-256 `b1f21fabbfea29df8b69ad69394e0dd643836a27801465425563cf8383d37352`. A second live browser journey selected the 44,407,621-byte `dongo-multipart-screen-recording.mp4` on `DONGO-3`, observed the first attempt's interrupted state, retried the same draft to readiness, and submitted human comment `js7bs7crnwqsn16afnhcs91dxh8dgavp`. A fresh restricted Claude Code MCP read resolved `video/mp4` attachment `jh74dfz47hprqmjb7ht0s0qxdh8dhts5`; an authenticated agent fetch reproduced all bytes with source/download SHA-256 `e1c9d0a23759da0d428ad621d72be345daa82bcae93f7a20a9de5a7a118db033`. Installed-CLI sync then exported the note and opaque attachment ID with deterministic file hash `8b4a36461997e9f8d35f3f8f80c05adf9a5cf2e19c77e5b1a7824318fa05666e`; a second sync was byte-stable. Candidate `778eaed` adds lazy, bounded, MIME/size-validated Blob previews for safe raster types while retaining the secure download and reverting to the generic card on any error; SVG and non-images remain download-only. The complete 249-case browser matrix includes finalized pasted-comment preview and Blob URL cleanup. A fresh isolated live browser then signed into the owning test account, scrolled `DONGOE2E-2` into the lazy-load boundary, and visually rendered the exact 180×180 image with filename, byte count, `secure preview and download`, and explicit Download control. The complete DOM image used a `blob:` source and matching intrinsic/rendered dimensions. | Repeat the byte-exact R2/MCP and live preview proof only if the attachment contract changes. |
| Attention email | Proven in development | A real one-hour important escalation was dispatched once and delivered through the Wisepunk Resend account. A live human response was subsequently returned on the agent's next pull, and the final CLI/human golden journey independently proved response → next-pull continuation → finish. | Repeat the combined delayed-delivery journey on a future release candidate. |
| Native clients and push | Out of current goal; missing against full PRD | The shared notification contracts/providers and APNs/FCM server code exist, but native client source trees and enabled push credentials do not. | Deferred by the product owner for this dev-only goal. |
| Environment isolation | Proven for current development and landing origins | The canonical dev deploy ran Convex and all six Workers in dependency order, then 14/14 development smoke and 8/8 live boundary smoke passed. Production auth/API/MCP paths remain absent. | Provision staging, produce an immutable candidate, rehearse rollback, and only then promote the accepted application artifact to production. |
| Operational release | Partially in current goal | Development runbooks, CI, safe logs, health/readiness, and per-service observability configuration exist. CI enforces full invocation logs and bounded trace sampling across all six development Workers; the configuration is live and a real CLI request was observed with credentials redacted and no application content logged. A read-only Cloudflare dashboard audit found six unrelated policies and no dongo policy; the complete 54-type catalog has no Workers application alert scoped to `dev.dongo.so`. Exact-host Health Check notifications are technically suitable, but the `dongo.so` Health Checks page requires a Pro/Smart Shield upgrade. No plan, policy, recipient, or Health Check was changed, and a broad zone policy remains prohibited because it would mix development and production landing traffic. Candidate workflow `.github/workflows/dev-availability.yml` instead provides a credential-free, read-only, exact-development 14/14 smoke at off-hour minutes plus an explicit post-check synthetic failure input. Its static guard passes, but it has not been pushed, activated, or delivery-tested. CI also packs, inspects, installs, and executes the immutable CLI archive on clean Ubuntu and macOS runners using the minimum supported Node 20. Clean Debian/Node 20 proves exact-package installation, real headless OAuth approval, explicit denial, expiry, authenticated operation, owner-only external credentials, and clean server/local revocation without an OS helper. | With explicit approval, put the bounded workflow on the default branch, enable failure-only Actions email for its human owner, prove one synthetic failure delivery, one healthy rerun, and one scheduled run. Staging rollback, production publication, and production support sign-off are deferred. |
| Exact PRD V1 success journey | Proven through CLI, Codex, and generic MCP; Claude Intake and work/Attention portions proven; live image and interrupted-multipart video proven | One live CLI/human run completed text Intake → triage → claim → truthful Working → important human review/response → next pull → finish → artifact/outcome → deterministic repository Markdown. A separate Codex MCP/human run completed the same lifecycle with full replay evidence, actor attribution, report metadata, truthful terminal state, and a later human-authored image comment that Codex MCP retrieved byte-for-byte from R2. `DONGO-6` proves the same complete Intake → work → human Attention → next-pull continuation → finish semantics through generic MCP Inspector with exact replay and deterministic repository Markdown. Claude Code independently proves its two portions across `DONGO-7` Intake claim/create/triage and `DONGO-5` work/Attention/continuation/finish, with exact replay evidence and no unintended work start. The live media extension also proves interrupted multipart retry, human comment attribution, MCP discovery, and byte-exact agent retrieval for a 44,407,621-byte H.264 screen recording. | Complete Claude and Codex refresh/revoke/reauthorize proof. |

## Required execution order

1. Complete Claude Code Intake plus refresh/revoke/reauthorize and finish Codex refresh/revoke/reauthorize plus isolated repository-sync evidence. Generic MCP Inspector's complete lifecycle and deterministic export are proven on `DONGO-6`; Claude Code's work/Attention/continuation/finish and replay semantics are proven on `DONGO-5`.
2. Complete the repeatable manual VoiceOver gate. Public-root and authenticated-workspace lab performance plus exact-package one-link authorization/revocation are proven; the broader distribution-host matrix is deferred beyond the current dev-only goal.
3. Approve and prove the bounded credential-free development availability workflow's failure-only notification route. Do not create a broad zone policy or activate any alternate paid route without separate approval.
4. Accept the exact development candidate and record the remaining full-PRD work separately.

Until every in-scope development row above is proven against its required scope, the active build goal remains open. Native clients, push, staging, and production remain visible as deferred full-PRD work and are not completion conditions for this goal.
