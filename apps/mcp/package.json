{
  "name": "@dongo/mcp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "wrangler types --config wrangler.jsonc src/worker-configuration.d.ts && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@dongo/mcp": "0.1.0",
    "@modelcontextprotocol/server": "2.0.0"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.127.1"
  }
}
